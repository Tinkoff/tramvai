export enum hooks {
  SERVER_RENDER_HTML_RENDER = 'html:server:render',
  SERVER_RENDER_HTML_LOAD = 'html:server:load',
}
export const { SERVER_RENDER_HTML_RENDER } = hooks;
export const { SERVER_RENDER_HTML_LOAD } = hooks;

export const PARAMETER_DELIMITER = ':';
export const WILDCARD_REGEXP = /\*/;
export const HISTORY_FALLBACK_REGEXP = /<history-fallback>/;
export const PARAM_MATCH_REGEXP = /:.+$/;
export const PARAM_PARSER_REGEXP = /([^(?]+)(?:\((.+)\))?(\?)?/;

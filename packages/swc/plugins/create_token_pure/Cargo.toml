[package]
name = "create_token_pure"
version = "0.0.0-stub"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
swc_core = { version = "0.76.44", features = ["ecma_plugin_transform", "testing"] }
swc_ecma_parser = { version = "0.134.12" }

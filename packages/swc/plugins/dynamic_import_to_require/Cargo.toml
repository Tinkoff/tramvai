[package]
name = "dynamic_import_to_require"
version = "0.0.0-stub"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
swc_core = { version = "0.76.44", features = ["ecma_plugin_transform", "ecma_quote", "ecma_utils", "testing"] }
swc_ecma_parser = { version = "0.134.12" }

if_chain = { version = "1.0.2" }

[package]
name = "provider_stack"
version = "0.0.0-stub"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
swc_core = { version = "0.43.18", features = ["plugin_transform", "testing"] }
swc_common = { version = "0.29.14" }
swc_ecma_parser = { version = "0.122.15" }
if_chain = { version = "1.0.2" }

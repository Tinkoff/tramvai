import React from 'react';
import { useActions } from '@tramvai/state';
import { Button } from 'shared/ui';
import { Counter } from 'entities/counter';
import { IncrementCounterButton } from 'features/increment-counter';
import { navigateAction } from 'features/navigation';

export const MainPage = () => {
  // Binding the navigation action to the store
  const navigate = useActions(navigateAction);

  return (
    <div>
      Main Page{' '}
      <Button onClick={() => navigate('/second/')}>to second page</Button>
      <p>
        <Counter /> <IncrementCounterButton />
      </p>
    </div>
  );
};

export default MainPage;

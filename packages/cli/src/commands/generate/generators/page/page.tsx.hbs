import React from 'react';

import styles from './{{ name }}Page.module.css';

export type {{ properCase name }}PageProps = {
  title: string;
};

export const {{ properCase name }}Page = (props: {{ properCase name }}PageProps) => {
  const { title } = props;

  return <div className={styles.div}>{title}</div>;
};

import React from 'react';
import { render, screen } from '@testing-library/react';
import { {{ name }}Page } from './{{ properCase name }}Page';

describe('{{ properCase name }}Page', () => {
  it('snapshot', () => {
    render(<{{ properCase name }}Page title="Page" />);

    expect(screen.getByText('Page')).toBeTruthy();
  });
});

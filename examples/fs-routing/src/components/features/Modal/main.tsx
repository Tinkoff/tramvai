import React from 'react';
import styles from './main.css';

export const MainModal = () => {
  return <p className={styles.MainModal}>This is modal for index page!</p>;
};

export default MainModal;
